# Component 1: ingestion service dependencies.

# Docling: PDF / DOCX / PPTX / HTML ingestion. Pulls in torch, layout and
# TableFormer models (weights cached under DOCLING_ARTIFACTS_PATH /
# ~/.cache/docling; bind-mounted so they survive rebuilds).
docling>=2.0,<3.0
docling-core>=2.0

# Markdown path — proper token stream with line maps for char-span provenance.
markdown-it-py>=3.0

# HTTP ingest service.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Real-time single-requirement assessor (src/reqqa/realtime.py) — socket.io
# streaming server, runs on the uvicorn above. (httpx is used by the LLM client;
# it ships with fastapi's stack. requests/websocket-client are test-client only,
# not runtime deps.)
python-socketio>=5.11
